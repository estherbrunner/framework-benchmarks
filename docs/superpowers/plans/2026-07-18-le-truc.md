# Le Truc Framework App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a new `le-truc` app under `apps/` that implements the shared weather-app DOM contract using `@zeix/le-truc` (HTML-first reactive Web Components), registered in `frameworks.json` so all derived artifacts generate correctly.

**Architecture:** Vite + TypeScript. The full weather-app markup lives in `index.html` as plain DOM with every `data-testid` node pre-rendered; a single `<weather-app>` custom element defined via `defineComponent` queries those nodes with `first`/`all`, declares reactive props with `expose`, and returns `on`/`watch` effect descriptors. Forecast rows use `createList` + a `<template>`-clone reconcile `watch`. `weather-service.ts` and `weather-utils.ts` are typed ports of the Vue originals.

**Tech Stack:** `@zeix/le-truc` v2.2.x, Vite 5, TypeScript (strict, ESM), Playwright (shared suites only).

**Spec:** `docs/superpowers/specs/2026-07-18-le-truc-design.md`

---

## Reference facts the engineer needs

- **Repo root:** `/Users/estherbrunner/Documents/GitHub/framework-benchmarks`. All paths below are relative to it unless noted.
- **Source-of-truth file:** `frameworks.json` drives root `package.json` scripts, website, CI matrix, and per-app READMEs. Never hand-edit the auto-generated artifacts.
- **Shared assets** live in `assets/` and are synced into each app's `assetsDir` by `npm run sync-assets`. The four stylesheets are `assets/styles/{design-system,variables,base,components}.css`, referenced from HTML as `/styles/<name>.css`. Mock data is `assets/mocks/weather-data.json`, fetched at runtime as `/mocks/weather-data.json`.
- **Dev server convention:** port 3000, base `http://localhost:3000/?mock=true` (Playwright appends `?mock=true`).
- **localStorage key:** exactly `weather-app-location`.
- **Mock-mode detection** (must match other apps): use mock when URL contains `mock=true` **or** `navigator.userAgent` contains `Playwright` or `HeadlessChrome`; `mock=false` forces real API calls.
- **Mock data shape** (`assets/mocks/weather-data.json`):
  - `current`: `{ time, temperature_2m, relative_humidity_2m, apparent_temperature, weather_code, cloud_cover, surface_pressure, wind_direction_10m, wind_speed_10m }`
  - `daily`: `{ time[], temperature_2m_max[], temperature_2m_min[], weather_code[], sunrise[], sunset[], rain_sum[], uv_index_max[], precipitation_probability_max[] }` — 7 entries each.
  - `WeatherService.getWeatherByCity` returns the API JSON plus `locationName` and `country` as top-level fields.
- **Le Truc API cheatsheet** (v2.2, functional form):
  - `defineComponent(name, factory)` — registers a custom element; `name` must contain a hyphen.
  - Factory receives `{ expose, first, all, host, on, watch, each }`.
  - `expose({ propName: initialValue })` — declares reactive props; they also become real properties on `host`.
  - `first(selector, hint?)` / `all(selector)` — query descendants; `first` throws if a hint is supplied and nothing matches.
  - `on(target, type, handler)` — event listener; handler returns a partial `{ propName: newValue }` to update exposed props, or mutates signals directly. Auto-disposed on disconnect.
  - `watch(source, updater)` — reactive effect; `source` is a prop-name string, a signal, or a `() => value`. `updater` is a DOM binder or `(value) => void`.
  - DOM binders: `bindText(el)`, `bindClass(el, className)`, `bindAttribute(el, attr)`, `bindVisible(el)` (sets `el.hidden`), `bindProperty(el, prop)`.
  - `each(elements, el => effect)` — runs a reactive scope per element; disposes when the element leaves.
  - `createList([], { keyConfig: 'item' })` — keyed reactive list; auto-keys `item0`, `item1`, … Methods: `.add(value)`, `.remove(key)`, `.byKey(key)`, `.keys()`.
- **Le Truc is ESM-only** and needs no Vite plugin. Optional dev warnings via `define: { 'process.env.DEV_MODE': '"true"' }` in `vite.config.js`.
- **Existing reference app:** `apps/vue/` — read its `src/services/weatherService.js` and `src/utils/weatherUtils.js` for the exact logic to port.

---

## File Structure

| File | Responsibility |
|---|---|
| `apps/le-truc/package.json` | App manifest; deps `@zeix/le-truc`; devDeps `vite`, `typescript`, `@playwright/test`; local fallback scripts. |
| `apps/le-truc/tsconfig.json` | Strict ESM TS config for Vite/esbuild. |
| `apps/le-truc/vite.config.js` | Vite config: `base: './'`, port 3000, `DEV_MODE` define. |
| `apps/le-truc/index.html` | Static weather-app markup with all `data-testid` nodes; `<weather-app>` root; `<template id="forecast-item-template">`; loads `/src/main.ts`. |
| `apps/le-truc/src/main.ts` | Entry; imports `./weather-app.ts` (side-effect registers the component). |
| `apps/le-truc/src/weather-app.ts` | The `defineComponent('weather-app', …)` factory — queries, props, effects, forecast reconcile, search, persistence. |
| `apps/le-truc/src/weather-service.ts` | Typed port of `apps/vue/src/services/weatherService.js`. |
| `apps/le-truc/src/weather-utils.ts` | Typed port of `apps/vue/src/utils/weatherUtils.js`. |
| `apps/le-truc/public/` | Empty dir; populated by `npm run sync-assets`. |
| `frameworks.json` | Add one `le-truc` entry. |
| `tests/config/playwright-le-truc.config.js` | New one-liner config. |
| `tests/config/playwright.config.base.js` | Add `le-truc` to the `configs` map. |
| Root `package.json` | Auto-regenerated by `npm run generate-scripts` — do NOT hand-edit. |

---

## Task 1: Scaffold app directory and config files

**Files:**
- Create: `apps/le-truc/package.json`
- Create: `apps/le-truc/tsconfig.json`
- Create: `apps/le-truc/vite.config.js`
- Create: `apps/le-truc/public/.gitkeep`

- [ ] **Step 1: Create `apps/le-truc/package.json`**

```json
{
  "name": "weather-app-le-truc",
  "version": "1.0.0",
  "description": "Weather app built with Le Truc",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "@zeix/le-truc": "^2.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.4.0",
    "vite": "^5.0.0"
  },
  "keywords": ["weather", "le-truc", "web-components", "frontend"],
  "license": "MIT"
}
```

- [ ] **Step 2: Create `apps/le-truc/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "types": []
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Create `apps/le-truc/vite.config.js`**

```js
import { defineConfig } from 'vite';

export default defineConfig({
  base: './',
  server: {
    port: 3000
  },
  define: {
    'process.env.DEV_MODE': '"true"'
  }
});
```

- [ ] **Step 4: Create empty `apps/le-truc/public/.gitkeep`**

Empty file (just `touch` it). This ensures the `public/` directory exists in git; `npm run sync-assets` will populate it later.

- [ ] **Step 5: Verify the structure**

Run: `ls -la apps/le-truc/`
Expected: `package.json`, `tsconfig.json`, `vite.config.js`, `public/`

- [ ] **Step 6: Commit**

```bash
git add apps/le-truc/package.json apps/le-truc/tsconfig.json apps/le-truc/vite.config.js apps/le-truc/public/.gitkeep
git commit -m "feat(le-truc): scaffold app directory with package.json, tsconfig, vite config"
```

---

## Task 2: Port `weather-utils.ts` from Vue

**Files:**
- Create: `apps/le-truc/src/weather-utils.ts`
- Reference: `apps/vue/src/utils/weatherUtils.js`

- [ ] **Step 1: Create `apps/le-truc/src/weather-utils.ts`**

This is a typed port of the Vue utility class. Logic is identical; only types are added.

```ts
export class WeatherUtils {
  static getWeatherDescription(weatherCode: number): string {
    const weatherCodes: Record<number, string> = {
      0: 'Clear sky',
      1: 'Mainly clear',
      2: 'Partly cloudy',
      3: 'Overcast',
      45: 'Fog',
      48: 'Depositing rime fog',
      51: 'Light drizzle',
      53: 'Moderate drizzle',
      55: 'Dense drizzle',
      56: 'Light freezing drizzle',
      57: 'Dense freezing drizzle',
      61: 'Slight rain',
      63: 'Moderate rain',
      65: 'Heavy rain',
      66: 'Light freezing rain',
      67: 'Heavy freezing rain',
      71: 'Slight snow fall',
      73: 'Moderate snow fall',
      75: 'Heavy snow fall',
      77: 'Snow grains',
      80: 'Slight rain showers',
      81: 'Moderate rain showers',
      82: 'Violent rain showers',
      85: 'Slight snow showers',
      86: 'Heavy snow showers',
      95: 'Thunderstorm',
      96: 'Thunderstorm with slight hail',
      99: 'Thunderstorm with heavy hail'
    };
    return weatherCodes[weatherCode] || 'Unknown';
  }

  static getWeatherIcon(weatherCode: number, isDay: number = 1): string {
    const iconMap: Record<number, string> = {
      0: isDay ? '☀️' : '🌙',
      1: isDay ? '🌤️' : '🌙',
      2: isDay ? '⛅' : '☁️',
      3: '☁️',
      45: '🌫️',
      48: '🌫️',
      51: '🌦️',
      53: '🌦️',
      55: '🌦️',
      56: '🌨️',
      57: '🌨️',
      61: '🌧️',
      63: '🌧️',
      65: '🌧️',
      66: '🌨️',
      67: '🌨️',
      71: '❄️',
      73: '❄️',
      75: '❄️',
      77: '❄️',
      80: '🌦️',
      81: '🌦️',
      82: '⛈️',
      85: '🌨️',
      86: '🌨️',
      95: '⛈️',
      96: '⛈️',
      99: '⛈️'
    };
    return iconMap[weatherCode] || '🌤️';
  }

  static getConditionClass(weatherCode: number): string {
    if ([0, 1].includes(weatherCode)) return 'clear';
    if ([2, 3].includes(weatherCode)) return 'cloudy';
    if ([45, 48].includes(weatherCode)) return 'foggy';
    if ([51, 53, 55, 56, 57, 61, 63, 65, 66, 67, 80, 81, 82].includes(weatherCode)) return 'rainy';
    if ([71, 73, 75, 77, 85, 86].includes(weatherCode)) return 'snowy';
    if ([95, 96, 99].includes(weatherCode)) return 'stormy';
    return 'clear';
  }

  static formatTemperature(temp: number): string {
    return `${Math.round(temp)}°C`;
  }

  static formatWindSpeed(speed: number): string {
    return `${Math.round(speed)} km/h`;
  }

  static formatPressure(pressure: number): string {
    return `${Math.round(pressure)} hPa`;
  }

  static formatPercentage(value: number): string {
    return `${Math.round(value)}%`;
  }

  static getWindDirection(degrees: number): string {
    const directions = ['N', 'NNE', 'NE', 'ENE', 'E', 'ESE', 'SE', 'SSE', 'S', 'SSW', 'SW', 'WSW', 'W', 'WNW', 'NW', 'NNW'];
    const index = Math.round(degrees / 22.5) % 16;
    return directions[index];
  }

  static formatDate(dateString: string): string {
    const date = new Date(dateString);
    return date.toLocaleDateString('en-US', {
      weekday: 'short',
      month: 'short',
      day: 'numeric'
    });
  }

  static formatTime(timeString: string): string {
    const date = new Date(timeString);
    return date.toLocaleTimeString('en-US', {
      hour: '2-digit',
      minute: '2-digit',
      hour12: false
    });
  }
}
```

- [ ] **Step 2: Typecheck**

Run: `cd apps/le-truc && npx tsc --noEmit`
Expected: exits 0 (no errors). If `tsc` is not installed yet, run `npm install` first in `apps/le-truc/` — it's needed for the next tasks anyway.

- [ ] **Step 3: Commit**

```bash
git add apps/le-truc/src/weather-utils.ts
git commit -m "feat(le-truc): add typed WeatherUtils port"
```

---

## Task 3: Port `weather-service.ts` from Vue

**Files:**
- Create: `apps/le-truc/src/weather-service.ts`
- Reference: `apps/vue/src/services/weatherService.js`

- [ ] **Step 1: Create `apps/le-truc/src/weather-service.ts`**

Typed port of the Vue service. Logic (including mock-mode detection rules, mock geocoding map, invalid-city handling, 200ms test delay, and exact error strings) is identical. Types added for `WeatherData` and `GeocodedLocation`.

```ts
export interface GeocodedLocation {
  latitude: number;
  longitude: number;
  name: string;
  country: string;
}

export interface WeatherData {
  latitude: number;
  longitude: number;
  current: {
    time: string;
    temperature_2m: number;
    relative_humidity_2m: number;
    apparent_temperature: number;
    weather_code: number;
    cloud_cover: number;
    surface_pressure: number;
    wind_direction_10m: number;
    wind_speed_10m: number;
    is_day?: number;
    pressure_msl?: number;
  };
  daily: {
    time: string[];
    temperature_2m_max: number[];
    temperature_2m_min: number[];
    weather_code: number[];
    sunrise: string[];
    sunset: string[];
    rain_sum: number[];
    uv_index_max: number[];
    precipitation_probability_max: number[];
  };
  locationName: string;
  country: string;
}

class WeatherService {
  baseUrl = 'https://api.open-meteo.com/v1';
  geocodingUrl = 'https://geocoding-api.open-meteo.com/v1';
  useMockData = this.shouldUseMockData();

  shouldUseMockData(): boolean {
    const isTestEnvironment = navigator.userAgent.includes('Playwright') ||
                              navigator.userAgent.includes('HeadlessChrome');
    if (window.location.search.includes('mock=false')) {
      return false;
    }
    return window.location.search.includes('mock=true') || isTestEnvironment;
  }

  async getMockData(): Promise<any> {
    try {
      if (this.isTestEnvironment()) {
        await new Promise(resolve => setTimeout(resolve, 200));
      }
      const response = await fetch('/mocks/weather-data.json');
      if (!response.ok) {
        throw new Error('Failed to load mock data');
      }
      return await response.json();
    } catch (error) {
      console.error('Error loading mock data:', error);
      throw error;
    }
  }

  isTestEnvironment(): boolean {
    return navigator.userAgent.includes('Playwright') ||
           navigator.userAgent.includes('HeadlessChrome');
  }

  getMockGeocodingData(cityName: string): GeocodedLocation {
    const mockCities: Record<string, GeocodedLocation> = {
      'London': { latitude: 51.5074, longitude: -0.1278, name: 'London', country: 'United Kingdom' },
      'Tokyo': { latitude: 35.6762, longitude: 139.6503, name: 'Tokyo', country: 'Japan' },
      'Paris': { latitude: 48.8566, longitude: 2.3522, name: 'Paris', country: 'France' },
      'São Paulo': { latitude: -23.5505, longitude: -46.6333, name: 'São Paulo', country: 'Brazil' },
      'New York': { latitude: 40.7128, longitude: -74.0060, name: 'New York', country: 'United States' },
      'Zürich': { latitude: 47.3769, longitude: 8.5417, name: 'Zürich', country: 'Switzerland' }
    };

    if (cityName.includes('Invalid') || cityName.includes('123') || !cityName.trim()) {
      throw new Error('Unable to find location. Please check the city name and try again.');
    }
    return mockCities[cityName] || mockCities['London'];
  }

  async geocodeLocation(cityName: string): Promise<GeocodedLocation> {
    if (this.useMockData) {
      return this.getMockGeocodingData(cityName);
    }
    try {
      const response = await fetch(
        `${this.geocodingUrl}/search?name=${encodeURIComponent(cityName)}&count=1&language=en&format=json`
      );
      if (!response.ok) {
        throw new Error('Failed to geocode location');
      }
      const data = await response.json();
      if (!data.results || data.results.length === 0) {
        throw new Error(`Location "${cityName}" not found. Please check the spelling and try again.`);
      }
      return data.results[0];
    } catch (error) {
      console.error('Geocoding error:', error);
      throw error;
    }
  }

  async getWeatherData(lat: number, lon: number): Promise<any> {
    if (this.useMockData) {
      return this.getMockData();
    }
    try {
      const response = await fetch(
        `${this.baseUrl}/forecast?` +
        `latitude=${lat}&longitude=${lon}&` +
        `current=temperature_2m,apparent_temperature,relative_humidity_2m,wind_speed_10m,wind_direction_10m,pressure_msl,cloud_cover,precipitation,weather_code,is_day&` +
        `daily=temperature_2m_max,temperature_2m_min,weather_code,sunrise,sunset,precipitation_probability_max,rain_sum,uv_index_max&` +
        `timezone=auto`
      );
      if (!response.ok) {
        throw new Error('Failed to fetch weather data');
      }
      return await response.json();
    } catch (error) {
      console.error('Weather API error:', error);
      throw error;
    }
  }

  async getWeatherByCity(cityName: string): Promise<WeatherData> {
    try {
      const location = await this.geocodeLocation(cityName);
      const weather = await this.getWeatherData(location.latitude, location.longitude);
      return {
        ...weather,
        locationName: location.name,
        country: location.country
      };
    } catch (error) {
      console.error('Weather service error:', error);
      throw error;
    }
  }
}

export const weatherService = new WeatherService();
```

> Note: the error strings `'Unable to find location. Please check the city name and try again.'` and `'Failed to fetch weather data'` are deliberate — the shared test regex is `/Unable to (find location|fetch weather data)/i`.

- [ ] **Step 2: Typecheck**

Run: `cd apps/le-truc && npx tsc --noEmit`
Expected: exits 0.

- [ ] **Step 3: Commit**

```bash
git add apps/le-truc/src/weather-service.ts
git commit -m "feat(le-truc): add typed WeatherService port"
```

---

## Task 4: Write `index.html` with full static markup

**Files:**
- Create: `apps/le-truc/index.html`

This file contains **all** required `data-testid` nodes as static HTML. Le Truc will enhance them in place — none of these are rendered by JS.

- [ ] **Step 1: Create `apps/le-truc/index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App - Le Truc</title>
    <link rel="stylesheet" href="/styles/design-system.css">
    <link rel="stylesheet" href="/styles/variables.css">
    <link rel="stylesheet" href="/styles/base.css">
    <link rel="stylesheet" href="/styles/components.css">
</head>
<body>
    <weather-app>
        <header class="header">
            <div class="container">
                <h1 class="header__title">Weather Front</h1>
            </div>
        </header>

        <main class="main">
            <div class="container">
                <section class="search-section">
                    <form class="search-form" data-testid="search-form">
                        <div class="search-form__group">
                            <label for="location-input" class="sr-only">Enter city name</label>
                            <input
                                id="location-input"
                                type="text"
                                class="search-input"
                                placeholder="Enter city name..."
                                data-testid="search-input"
                                autocomplete="off"
                            />
                            <button type="submit" class="search-button" data-testid="search-button">
                                <span class="search-button__text">Get Weather</span>
                                <span class="search-button__icon">🌦️</span>
                            </button>
                        </div>
                    </form>
                </section>

                <div class="weather-container" data-testid="weather-container">
                    <div class="loading" data-testid="loading" hidden>
                        <div class="loading__spinner"></div>
                        <p>Loading weather data...</p>
                    </div>

                    <div class="error" data-testid="error" hidden>
                        <h2 class="error__title">Unable to fetch weather data</h2>
                        <p class="error__message"></p>
                    </div>

                    <div class="weather-content" data-testid="weather-content" hidden>
                        <div class="weather-layout">
                            <section class="current-section">
                                <h2 class="section-title">Current Weather</h2>
                                <div class="weather-card" data-testid="current-weather">
                                    <div class="current-weather">
                                        <h3 class="current-weather__location" data-testid="current-location"></h3>
                                        <div class="current-weather__main">
                                            <div class="current-weather__icon" data-testid="current-icon"></div>
                                            <div class="current-weather__temp-group">
                                                <div class="current-weather__temp" data-testid="current-temperature"></div>
                                                <div class="current-weather__condition" data-testid="current-condition"></div>
                                            </div>
                                        </div>
                                        <div class="current-weather__details">
                                            <div class="weather-detail">
                                                <div class="weather-detail__label">Feels like</div>
                                                <div class="weather-detail__value" data-testid="feels-like"></div>
                                            </div>
                                            <div class="weather-detail">
                                                <div class="weather-detail__label">Humidity</div>
                                                <div class="weather-detail__value" data-testid="humidity"></div>
                                            </div>
                                            <div class="weather-detail">
                                                <div class="weather-detail__label">Wind Speed</div>
                                                <div class="weather-detail__value" data-testid="wind-speed"></div>
                                            </div>
                                            <div class="weather-detail">
                                                <div class="weather-detail__label">Pressure</div>
                                                <div class="weather-detail__value" data-testid="pressure"></div>
                                            </div>
                                            <div class="weather-detail">
                                                <div class="weather-detail__label">Cloud Cover</div>
                                                <div class="weather-detail__value" data-testid="cloud-cover"></div>
                                            </div>
                                            <div class="weather-detail">
                                                <div class="weather-detail__label">Wind Direction</div>
                                                <div class="weather-detail__value" data-testid="wind-direction"></div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </section>

                            <section class="forecast-section">
                                <h2 class="section-title">7-Day Forecast</h2>
                                <div class="forecast">
                                    <div class="forecast__list" data-testid="forecast-list"></div>
                                </div>
                            </section>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <footer class="footer">
            <div class="container">
                <p class="footer__text">
                    Built with Le Truc • MIT License •
                    <a href="https://github.com/Lissy93" class="footer__link" target="_blank" rel="noopener">Alicia Sykes</a>
                </p>
            </div>
        </footer>

        <template id="forecast-item-template">
            <div class="forecast-item" data-testid="forecast-item" tabindex="0">
                <div class="forecast-item__day"></div>
                <div class="forecast-item__icon"></div>
                <div class="forecast-item__info">
                    <div class="forecast-item__condition"></div>
                    <div class="forecast-item__temps" data-testid="forecast-temps">
                        <span class="forecast-item__high" data-testid="forecast-high"></span>
                        <span class="forecast-item__low" data-testid="forecast-low"></span>
                    </div>
                </div>
            </div>
        </template>
    </weather-app>

    <script type="module" src="/src/main.ts"></script>
</body>
</html>
```

- [ ] **Step 2: Verify the file is well-formed**

Run: `python3 -c "import html.parser as h; h.HTMLParser().feed(open('apps/le-truc/index.html').read()); print('OK')"`
Expected: prints `OK`.

- [ ] **Step 3: Commit**

```bash
git add apps/le-truc/index.html
git commit -m "feat(le-truc): add static weather-app markup with full data-testid contract"
```

---

## Task 5: Write `src/main.ts`

**Files:**
- Create: `apps/le-truc/src/main.ts`

- [ ] **Step 1: Create `apps/le-truc/src/main.ts`**

```ts
import './weather-app.js';
```

> The `.js` extension is correct even though the source is `.ts`: Vite/esbuild resolves the `.ts` file and emits JS. TypeScript with `moduleResolution: bundler` accepts this.

- [ ] **Step 2: Commit**

```bash
git add apps/le-truc/src/main.ts
git commit -m "feat(le-truc): add main.ts entry point"
```

---

## Task 6: Write `src/weather-app.ts` — the component factory

**Files:**
- Create: `apps/le-truc/src/weather-app.ts`

This is the core of the app. It defines a single `<weather-app>` custom element that enhances the static HTML.

- [ ] **Step 1: Create `apps/le-truc/src/weather-app.ts`**

```ts
import {
  defineComponent,
  createList,
  bindVisible,
  bindClass,
  type Store
} from '@zeix/le-truc';
import { weatherService, type WeatherData } from './weather-service.js';
import { WeatherUtils } from './weather-utils.js';

interface ForecastItemData {
  date: string;
  high: number;
  low: number;
  weatherCode: number;
  sunrise: string;
  sunset: string;
  rainSum: number;
  uvIndex: number;
  precipitationProb: number;
}

interface WeatherAppProps {
  city: string;
  isLoading: boolean;
  error: string | null;
  weatherData: WeatherData | null;
  activeIndex: number | null;
}

defineComponent<WeatherAppProps>('weather-app', ({ expose, first, all, host, on, watch }) => {

  // --- Query all required DOM nodes ---
  const form = first('form[data-testid="search-form"]', 'Search form is required');
  const input = first('input[data-testid="search-input"]', 'Search input is required') as HTMLInputElement;
  const button = first('button[data-testid="search-button"]', 'Search button is required') as HTMLButtonElement;
  const loadingEl = first('[data-testid="loading"]', 'Loading element is required') as HTMLElement;
  const errorEl = first('[data-testid="error"]', 'Error element is required') as HTMLElement;
  const errorMessageEl = first('.error__message', 'Error message element is required') as HTMLElement;
  const contentEl = first('[data-testid="weather-content"]', 'Weather content element is required') as HTMLElement;
  const locationEl = first('[data-testid="current-location"]', 'Current location element is required') as HTMLElement;
  const tempEl = first('[data-testid="current-temperature"]', 'Current temperature element is required') as HTMLElement;
  const iconEl = first('[data-testid="current-icon"]', 'Current icon element is required') as HTMLElement;
  const conditionEl = first('[data-testid="current-condition"]', 'Current condition element is required') as HTMLElement;
  const feelsLikeEl = first('[data-testid="feels-like"]', 'Feels-like element is required') as HTMLElement;
  const humidityEl = first('[data-testid="humidity"]', 'Humidity element is required') as HTMLElement;
  const windSpeedEl = first('[data-testid="wind-speed"]', 'Wind-speed element is required') as HTMLElement;
  const pressureEl = first('[data-testid="pressure"]', 'Pressure element is required') as HTMLElement;
  const cloudCoverEl = first('[data-testid="cloud-cover"]', 'Cloud-cover element is required') as HTMLElement;
  const windDirectionEl = first('[data-testid="wind-direction"]', 'Wind-direction element is required') as HTMLElement;
  const forecastListEl = first('[data-testid="forecast-list"]', 'Forecast list element is required') as HTMLElement;
  const template = first('#forecast-item-template', 'Forecast item template is required') as HTMLTemplateElement;

  // --- Reactive list of forecast items, keyed by index string ---
  const forecastList = createList<ForecastItemData>([], { keyConfig: 'item' });

  // --- Declare reactive props ---
  let initialCity = 'London';
  try {
    const saved = localStorage.getItem('weather-app-location');
    if (saved) initialCity = saved;
  } catch {
    /* localStorage unavailable; default to London */
  }

  expose({
    city: initialCity,
    isLoading: false,
    error: null,
    weatherData: null,
    activeIndex: null
  });

  // --- Load weather for a city; updates props + forecast list ---
  const loadWeather = async (cityName: string) => {
    host.isLoading = true;
    host.error = null;
    host.weatherData = null;
    host.city = cityName;
    try {
      const data = await weatherService.getWeatherByCity(cityName);
      host.weatherData = data;
      input.value = cityName;
      try {
        localStorage.setItem('weather-app-location', cityName);
      } catch {
        /* ignore persistence errors */
      }

      // Rebuild forecast list from daily arrays
      const daily = data.daily;
      for (const key of Array.from(forecastList.keys())) forecastList.remove(key);
      for (let i = 0; i < daily.time.length; i++) {
        forecastList.add({
          date: daily.time[i],
          high: daily.temperature_2m_max[i],
          low: daily.temperature_2m_min[i],
          weatherCode: daily.weather_code[i],
          sunrise: daily.sunrise[i],
          sunset: daily.sunset[i],
          rainSum: daily.rain_sum[i],
          uvIndex: daily.uv_index_max[i],
          precipitationProb: daily.precipitation_probability_max[i]
        });
      }
      host.activeIndex = null;
    } catch (err: any) {
      host.error = err?.message || 'Failed to fetch weather data';
    } finally {
      host.isLoading = false;
    }
  };

  // --- Forecast DOM reconciler: mirror list keys into forecastListEl ---
  watch(() => Array.from(forecastList.keys()), keys => {
    const current = new Map<string, HTMLElement>();
    for (const child of Array.from(forecastListEl.children)) {
      const el = child as HTMLElement;
      const key = el.dataset.key;
      if (key) current.set(key, el);
    }
    const keysSet = new Set(keys);

    // Drop children whose key is no longer present
    for (const [key, el] of current) {
      if (!keysSet.has(key)) el.remove();
    }

    // Insert new keys (clone template) and move existing into order
    for (let i = 0; i < keys.length; i++) {
      const key = keys[i];
      let el = key && current.get(key);
      if (key && !el) {
        const fragment = template.content.cloneNode(true) as DocumentFragment;
        el = fragment.firstElementChild as HTMLElement;
        el.dataset.key = key;
        const datum = forecastList.byKey(key)?.get();
        if (datum) {
          (el.querySelector('.forecast-item__day') as HTMLElement).textContent = WeatherUtils.formatDate(datum.date);
          (el.querySelector('.forecast-item__icon') as HTMLElement).textContent = WeatherUtils.getWeatherIcon(datum.weatherCode, 1);
          (el.querySelector('.forecast-item__condition') as HTMLElement).textContent = WeatherUtils.getWeatherDescription(datum.weatherCode);
          (el.querySelector('[data-testid="forecast-high"]') as HTMLElement).textContent = WeatherUtils.formatTemperature(datum.high);
          (el.querySelector('[data-testid="forecast-low"]') as HTMLElement).textContent = WeatherUtils.formatTemperature(datum.low);
        }
      }
      const currentAtI = forecastListEl.children[i];
      if (el && currentAtI !== el) forecastListEl.insertBefore(el, currentAtI ?? null);
    }
  });

  // --- Per-item active-state toggle (re-evaluated when activeIndex changes) ---
  watch(() => Array.from(forecastList.keys()), keys => {
    const items = Array.from(forecastListEl.querySelectorAll<HTMLElement>('.forecast-item'));
    for (const item of items) {
      const idx = Number(item.dataset.key.replace('item', ''));
      // toggles .active class based on activeIndex
      const update = () => { item.classList.toggle('active', host.activeIndex === idx); };
      update();
      watch(() => host.activeIndex, update);
    }
  });

  // --- Click handler for forecast items (event delegation on the list) ---
  on(forecastListEl, 'click', (_e: Event, target: HTMLElement) => {
    const item = target.closest('.forecast-item') as HTMLElement | null;
    if (!item?.dataset.key) return {};
    const idx = Number(item.dataset.key.replace('item', ''));
    return { activeIndex: host.activeIndex === idx ? null : idx };
  });

  // --- Visibility of loading / error / content ---
  watch('isLoading', isLoading => { loadingEl.hidden = !isLoading; });
  watch('error', err => {
    errorEl.hidden = !err;
    if (err) errorMessageEl.textContent = err;
  });
  watch('weatherData', data => { contentEl.hidden = !data; });

  // --- Disable search button + sync button text while loading ---
  watch('isLoading', isLoading => { button.disabled = isLoading; });
  const buttonTextEl = first('.search-button__text', 'Search button text is required') as HTMLElement;
  watch('isLoading', isLoading => {
    buttonTextEl.textContent = isLoading ? 'Loading...' : 'Get Weather';
  });

  // --- Populate current-weather fields whenever data changes ---
  watch('weatherData', data => {
    if (!data) return;
    const c = data.current;
    locationEl.textContent = `${data.locationName}${data.country ? `, ${data.country}` : ''}`;
    tempEl.textContent = WeatherUtils.formatTemperature(c.temperature_2m);
    iconEl.textContent = WeatherUtils.getWeatherIcon(c.weather_code, c.is_day ?? 1);
    conditionEl.textContent = WeatherUtils.getWeatherDescription(c.weather_code);
    conditionEl.className = `current-weather__condition ${WeatherUtils.getConditionClass(c.weather_code)}`;
    feelsLikeEl.textContent = WeatherUtils.formatTemperature(c.apparent_temperature);
    humidityEl.textContent = WeatherUtils.formatPercentage(c.relative_humidity_2m);
    windSpeedEl.textContent = WeatherUtils.formatWindSpeed(c.wind_speed_10m);
    pressureEl.textContent = WeatherUtils.formatPressure(c.pressure_msl ?? c.surface_pressure);
    cloudCoverEl.textContent = WeatherUtils.formatPercentage(c.cloud_cover);
    windDirectionEl.textContent = WeatherUtils.getWindDirection(c.wind_direction_10m);
  });

  // --- Search submit handler ---
  on(form, 'submit', (e: Event) => {
    e.preventDefault();
    const city = input.value.trim();
    if (city) loadWeather(city);
    return {};
  });

  // --- Trigger initial load ---
  loadWeather(host.city);
});
```

> **Important API notes for the engineer:**
> - If `Store` is not exported from `@zeix/le-truc`, remove it from the import — it is not used directly above (kept for type reference only if available). Prefer omitting unused imports to satisfy `noUnusedLocals`.
> - The exact reactive-list item value type returned by `forecastList.byKey(key)` may be a `Store`/signal-like object exposing `.get()` — the code calls `.get()` defensively. If TypeScript complains, cast via `(forecastList.byKey(key) as any)?.get()`.
> - The `on(...)` handler signature `(event, target) => partial props` is the Le Truc v2 form. If the installed version uses a different signature, consult https://zeixcom.github.io/le-truc/ and adjust — but keep behavior identical.

- [ ] **Step 2: Typecheck**

Run: `cd apps/le-truc && npx tsc --noEmit`
Expected: exits 0. If errors mention missing `Store` export or `on` signature mismatch, follow the notes above (remove unused import / cast).

- [ ] **Step 3: Commit**

```bash
git add apps/le-truc/src/weather-app.ts
git commit -m "feat(le-truc): implement weather-app component with reactive forecast reconcile"
```

---

## Task 7: Install dependencies

**Files:** none (installs into `apps/le-truc/node_modules`)

- [ ] **Step 1: Run npm install**

Run: `cd apps/le-truc && npm install`
Expected: installs `@zeix/le-truc`, `vite`, `typescript`, `@playwright/test` without errors.

- [ ] **Step 2: Verify Le Truc is installed and inspect its real exports**

Run: `node -e "import('@zeix/le-truc').then(m => console.log(Object.keys(m).sort()))" --input-type=module`
Expected: prints a list including `defineComponent`, `createList`, `bindVisible`, `bindClass`. If any name used in Task 6 is absent or differently named, note it for the Task 6 fix step.

- [ ] **Step 3: Re-typecheck after install (resolves real types)**

Run: `cd apps/le-truc && npx tsc --noEmit`
Expected: exits 0. If errors appear (e.g. `Store` not exported, `on` signature differs, `createList` generic arity differs), fix `src/weather-app.ts` accordingly and re-run until clean. This is the moment to reconcile the implementation against the library's actual runtime types.

- [ ] **Step 4: Commit any fixes from Step 3**

```bash
git add apps/le-truc/src/weather-app.ts
git commit -m "fix(le-truc): align component with installed @zeix/le-truc types" 
```
(Only if changes were needed; otherwise skip.)

---

## Task 8: Register the framework in `frameworks.json`

**Files:**
- Modify: `frameworks.json`

- [ ] **Step 1: Add the `le-truc` entry**

Open `frameworks.json`. Inside the `"frameworks": [ ... ]` array, add this object (place it after the `lit` entry to keep alphabetical-ish grouping; exact position is not load-bearing):

```json
{
  "id": "le-truc",
  "name": "Le Truc",
  "displayName": "Le Truc",
  "dir": "le-truc",
  "assetsDir": "public",
  "build": {
    "buildCommand": "vite build",
    "hasNodeModules": true,
    "devCommand": "vite",
    "testCommand": "playwright test --config=tests/config/playwright-le-truc.config.js",
    "lintFiles": ["ts", "html"]
  },
  "meta": {
    "emoji": "🧩",
    "iconName": "zeix",
    "website": "https://zeixcom.github.io/le-truc/",
    "docs": "https://zeixcom.github.io/le-truc/",
    "github": "https://github.com/zeixcom/le-truc",
    "npmPackage": "@zeix/le-truc",
    "color": "#1E1E1E",
    "accentColor": "#E63946",
    "logo": "",
    "description": "Type-safe reactive Web Components layer over server-rendered HTML",
    "longDescription": "HTML-first progressive enhancement with fine-grained signals, built on standard Web Components."
  }
}
```

> The brand fields (`emoji`, `color`, `accentColor`, `iconName`) are placeholders. If Simple Icons has no `zeix` slug at validation time, replace `iconName` with `"iconUrl": "https://zeixcom.github.io/le-truc/favicon.png"` (or another absolute logo URL) — check https://simpleicons.org for the correct slug first.

- [ ] **Step 2: Validate the JSON**

Run: `python3 -c "import json; json.load(open('frameworks.json')); print('valid')"`
Expected: prints `valid`.

- [ ] **Step 3: Validate against the schema**

Run: `python3 scripts/verify/validate_schemas.py`
Expected: exits 0, no errors mentioning `le-truc`.

- [ ] **Step 4: Commit**

```bash
git add frameworks.json
git commit -m "feat(le-truc): register framework in frameworks.json"
```

---

## Task 9: Add Playwright config files

**Files:**
- Create: `tests/config/playwright-le-truc.config.js`
- Modify: `tests/config/playwright.config.base.js`

- [ ] **Step 1: Create `tests/config/playwright-le-truc.config.js`**

```js
const { createConfig } = require('./playwright.config.base.js');

module.exports = createConfig('le-truc');
```

- [ ] **Step 2: Add `le-truc` to the `configs` map in `playwright.config.base.js`**

Open `tests/config/playwright.config.base.js`. Find the `configs` object inside `createConfig`. Add this entry (mirror the existing per-framework entries; placement anywhere inside the object is fine):

```js
    'le-truc': {
      baseURL: 'http://localhost:3000/?mock=true',
      webServer: {
        command: 'npm run dev:le-truc',
        url: 'http://localhost:3000/',
      }
    },
```

- [ ] **Step 3: Commit**

```bash
git add tests/config/playwright-le-truc.config.js tests/config/playwright.config.base.js
git commit -m "feat(le-truc): add Playwright config and base webServer entry"
```

---

## Task 10: Regenerate root scripts and sync assets

**Files:**
- Modified (auto): root `package.json` (scripts block)
- Modified (auto): `apps/le-truc/public/styles/*`, `apps/le-truc/public/mocks/*`

- [ ] **Step 1: Regenerate root package.json scripts**

Run: `npm run generate-scripts`
Expected: completes without error. The root `package.json` now contains `dev:le-truc`, `build:le-truc`, `test:le-truc`, `lint:le-truc` and updated `:all` aggregators.

- [ ] **Step 2: Verify the new scripts exist**

Run: `node -e "const s=require('./package.json').scripts; console.log(['dev:le-truc','build:le-truc','test:le-truc','lint:le-truc'].filter(k => s[k]).join(', '))"`
Expected: prints `dev:le-truc, build:le-truc, test:le-truc, lint:le-truc`.

- [ ] **Step 3: Sync shared assets into the app**

Run: `npm run sync-assets`
Expected: completes without error. `apps/le-truc/public/styles/` now contains the 4 stylesheets; `apps/le-truc/public/mocks/weather-data.json` exists.

- [ ] **Step 4: Verify assets landed**

Run: `ls apps/le-truc/public/styles/ apps/le-truc/public/mocks/`
Expected: lists `base.css components.css design-system.css variables.css` and `weather-data.json`.

- [ ] **Step 5: Commit**

```bash
git add package.json apps/le-truc/public
git commit -m "feat(le-truc): regenerate root scripts and sync shared assets"
```

---

## Task 11: Smoke-test the dev server manually

**Files:** none

This catches runtime issues before running the full Playwright suite.

- [ ] **Step 1: Start the dev server in the background**

Run: `npm run dev:le-truc` (in a separate terminal, or background it)
Expected: Vite prints `Local: http://localhost:3000/` and serves with no errors.

- [ ] **Step 2: Fetch the page and check it serves**

Run: `curl -s http://localhost:3000/?mock=true | grep -o 'weather-app\|search-input\|forecast-item-template' | sort -u`
Expected: prints `forecast-item-template`, `search-input`, `weather-app`.

- [ ] **Step 3: Stop the dev server**

Stop the background server (Ctrl-C or kill the process).

---

## Task 12: Run the Playwright suite and fix failures

**Files:** potentially `apps/le-truc/src/weather-app.ts`, `apps/le-truc/index.html`

This is the validation gate. The shared suites define correctness.

- [ ] **Step 1: Run the full le-truc test suite**

Run: `npm run test:le-truc`
Expected: all tests in `tests/weather-app.test.js` and `tests/e2e/weather-app-advanced.test.js` pass. If failures appear, proceed to Step 2.

- [ ] **Step 2: Diagnose and fix failures (iterate)**

Common failure patterns and fixes:
- **`[data-testid="loading"]` never visible** — the loading `watch` may be firing after the fetch resolves. Confirm `loadingEl.hidden` starts `true` in HTML (Task 4) and flips `false` synchronously when `host.isLoading = true`. Note: the test for the initial loading state is a no-op (commented out in the suite), but the city-search test does check `[data-testid="loading"]` visibility in a try/catch — a miss there is tolerated.
- **`current-temperature` mismatch > ±3** — confirm `tempEl.textContent` is set to `WeatherUtils.formatTemperature(c.temperature_2m)` (which yields e.g. `"16°C"`), and the element contains nothing else.
- **Forecast count != 7** — confirm the reconciler isn't double-adding. The `for…of` loop in `loadWeather` must `remove` all existing keys before `add`ing new ones.
- **`.active` class not toggling** — confirm the delegated `on(forecastListEl, 'click', …)` returns `{ activeIndex: … }` and the per-item `watch(() => host.activeIndex, update)` runs.
- **`error` not visible / text mismatch** — confirm error strings are exactly `'Unable to find location…'` or `'Failed to fetch weather data'`.
- **`search-input` not persisting `London` after reload** — confirm `input.value = cityName` is set inside `loadWeather` (not only on submit) and `localStorage.setItem` runs before the test reloads.

For each failure: read the error, identify which `data-testid` or behavior is wrong, edit `src/weather-app.ts` (or `index.html` if the markup is wrong), re-run `npm run test:le-truc`.

- [ ] **Step 3: Re-run until green**

Run: `npm run test:le-truc`
Expected: all tests pass.

- [ ] **Step 4: Commit any fixes**

```bash
git add apps/le-truc
git commit -m "fix(le-truc): resolve Playwright test failures"
```
(Only if changes were needed; otherwise skip.)

---

## Task 13: Lint and final validation

**Files:** none (validation only)

- [ ] **Step 1: Run the linter**

Run: `npm run lint:le-truc`
Expected: no errors. Fix any lint issues in `apps/le-truc/src/*.ts` or `index.html` and re-run.

- [ ] **Step 2: Run repo-wide check**

Run: `npm run check`
Expected: exits 0. This validates schemas, configs, and consistency.

- [ ] **Step 3: Commit any lint fixes**

```bash
git add apps/le-truc
git commit -m "style(le-truc): apply lint fixes"
```
(Only if changes were needed; otherwise skip.)

---

## Task 14: Generate the app README (auto)

**Files:**
- Modified (auto): `apps/le-truc/README.md`

- [ ] **Step 1: Generate the README**

Run: `python3 scripts/transform/build_app_readme.py` (or whichever npm script wraps it — check root `package.json` for a `readme` or `docs` script)
Expected: `apps/le-truc/README.md` is created.

- [ ] **Step 2: Verify**

Run: `ls apps/le-truc/README.md`
Expected: file exists.

- [ ] **Step 3: Commit**

```bash
git add apps/le-truc/README.md
git commit -m "docs(le-truc): generate app README"
```

---

## Done

All 14 tasks complete. The `le-truc` framework app:
- Passes the shared Playwright suites in mock mode.
- Is registered in `frameworks.json` and flows through to scripts, Playwright config, CI matrix, website, and README automatically.
- Follows the repo's conventions (port 3000, `?mock=true`, localStorage key, shared styles + mock data).
- Uses Le Truc idiomatically (single HTML-first `<weather-app>` custom element, `createList` forecast reconcile).

**Do not push** — commits stay local on `feature/le-truc` until the user says otherwise.
